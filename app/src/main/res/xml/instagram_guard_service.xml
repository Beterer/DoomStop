<?xml version="1.0" encoding="utf-8"?>
<!--
  Events from Instagram only. Window content is retrievable so the guard can ask whether one
  of the Direct Messages view IDs is on screen; flagReportViewIds is what makes those IDs
  visible to the lookup. Nothing else about the screen is read.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/instagram_guard_description"
    android:packageNames="com.instagram.android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagReportViewIds"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="200"
    android:isAccessibilityTool="false" />
